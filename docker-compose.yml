services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flight_delay_api
    ports:
      - "8000:8000"
    volumes:
      # Override the baked-in model without rebuilding the image.
      - ./models:/app/models:ro
    env_file:
      - .env
    restart: unless-stopped
